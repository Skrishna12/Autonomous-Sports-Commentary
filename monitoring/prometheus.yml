global:
  scrape_interval: 10s

rule_files:
  - /etc/prometheus/prometheus-alerts.yml

scrape_configs:
  - job_name: ascs-api
    static_configs:
      - targets: ["api:8000"]
    metrics_path: /metrics
